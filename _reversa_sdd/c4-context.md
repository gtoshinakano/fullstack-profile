# C4 — Level 1: System Context

> Generated by Reversa Architect · 2026-05-17
> Re-extracted · 2026-05-20 (includes Feature 006 — Tech Stack Opinions)
> Confidence: 🟢 CONFIRMED | 🟡 INFERRED

---

## Diagram

```mermaid
C4Context
    title System Context — fullstack-profile Portfolio

    Person(recruiter, "Recruiter", "Hiring manager or technical recruiter in Japan, Brazil, or English-speaking countries. Evaluates the author's profile and may initiate contact.")
    Person(peer, "Technical Peer", "Developer or collaborator who reviews the project portfolio and technical skills.")
    Person(reader, "Curious Reader", "Developer interested in UI/UX — reads the '3-3-3 Principles' article.")
    Person(author, "Gabriel Toshinori Nakano", "Portfolio owner. Pushes code to main to trigger deployments.")

    System(portfolio, "fullstack-profile", "Static Next.js portfolio site. Presents professional profile, project history, and a UI/UX article. Supports EN, JA, PT-BR.")

    System_Ext(github_pages, "GitHub Pages", "Free static hosting. Serves the site at gtoshinakano.github.io/fullstack-profile/")
    System_Ext(github_actions, "GitHub Actions", "CI/CD pipeline. Builds the Next.js static export and deploys to the gh-pages branch on every push to main.")
    System_Ext(ga4, "Google Analytics 4", "Tracks visitor behavior: which profile tab was viewed, whether the article CTA was clicked. Production only.")
    System_Ext(pace, "Pace.js (CDN)", "Page load progress indicator. Fires a 'done' event that gates the hero section render.")
    System_Ext(unicons, "Unicons (CDN)", "Icon font set. Provides UI icons used in the language switcher and article decorations.")
    System_Ext(gfonts, "Google Fonts", "Serves the Syne typeface family used across the portfolio.")

    Rel(recruiter, portfolio, "Visits", "HTTPS / Browser")
    Rel(peer, portfolio, "Visits", "HTTPS / Browser")
    Rel(reader, portfolio, "Visits", "HTTPS / Browser")
    Rel(author, github_actions, "Pushes to main", "git push")

    Rel(github_actions, github_pages, "Deploys static files", "GitHub Pages Deploy Action")
    Rel(github_pages, portfolio, "Serves static assets", "HTTPS")

    Rel(portfolio, ga4, "Sends events", "HTTPS / gtag.js (prod only)")
    Rel(portfolio, pace, "Loads script", "HTTPS / CDN script tag")
    Rel(portfolio, unicons, "Loads icon font", "HTTPS / CDN CSS link")
    Rel(portfolio, gfonts, "Loads Syne font", "HTTPS / CSS link")
```

---

## Personas Detail

| Persona | Primary Interaction | Language | Key Feature Used |
|---------|-------------------|----------|-----------------|
| Recruiter | Reviews work history, reads the recruiter message | JA / PT-BR / EN | Profile tab (Jobs), Message tab (FuturePartner) |
| Technical Peer | Reviews project accordion | EN | Projects tab |
| Curious Reader | Reads "3-3-3 Principles" article | EN (only) | MainContent article |
| Author | Deploys via git push | — | GitHub Actions pipeline |

---

## Notes

- 🟢 The portfolio has **no backend server** — all external calls are browser-to-CDN or browser-to-GA
- 🟢 GA4 is **production-only** — gated by `process.env.NODE_ENV === 'production'`
- 🟡 Pace.js is loaded from a CDN script tag in the `<head>` — no version pinning; a CDN outage would prevent the page from revealing content
- 🟡 Unicons CDN outage would degrade icon rendering but would not break functionality
