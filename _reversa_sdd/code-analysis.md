# Code Analysis — fullstack-profile

> Generated by Reversa Archaeologist · 2026-05-17
> Confidence: 🟢 CONFIRMED | 🟡 INFERRED | 🔴 GAP
> doc_level: completo

---

## Executive Summary

`fullstack-profile` is a **single-page personal portfolio** for Gabriel Toshinori Nakano, a Full-Stack Developer based in Tokyo. The application is built with **Next.js 14 (Pages Router)** and deployed as a **static export to GitHub Pages**. It is composed of 7 distinct features analyzed below.

**Architecture pattern:** component-driven SPA with file-based routing, no API layer, no server-side state, no database. All data is embedded as static JSON.

---

## Feature 1 — hero-section

**Purpose:** Full-viewport animated mountain landscape that serves as the visual entry point. Responsive: renders `WideScreen` (landscape) or `Mobile` (portrait) based on viewport aspect ratio.

### Primary Files
| File | Role |
|------|------|
| `src/components/views/dev/gabriel/HeroSection/WideScreen.tsx` | Desktop/landscape parallax hero |
| `src/components/views/dev/gabriel/HeroSection/Mobile.tsx` | Mobile/portrait parallax hero |
| `src/components/views/dev/gabriel/HeroSection/HeroSvg.js` | Inline SVG text art (two variants) |
| `src/components/views/dev/gabriel/HeroSection/index.ts` | Barrel export |

### Functions & Components

**`WideScreen({ loading: boolean })`** 🟢
- On `loading === false`: initializes 10+ GSAP element positions, attaches ScrollTrigger timeline scrubbing 600px
- Layer z-order (bottom→top): background → `#big-mnt-img` (z=0) → `#uiux-dev-txt` (z=10) → `#mid-mnt-img` (z=30) → clouds (z=40/50) → `#sm-mnt-back` → `#climb-mnt` → `#sm-mnt-front` → `#cta-btn` (z=50)
- `handleAction()`: scrolls to `main` element + fires GA event `hero_cta` (category: "3-3-3 Principle", label: "widescreen")

**`Mobile({ loading: boolean })`** 🟢
- Same structure as WideScreen with adapted initial positions for portrait viewport
- `handleAction()` fires same GA event with label "mobile screen"

**`UXDevWide()`** / **`UXDevMobile()`** 🟢
- Pure SVG components rendering white-filled path text art: "FULL STACK DEV. | developer.p" (wide) and "FULL. ULL. DEV. | developer.p" (mobile)
- No external dependencies, no props

### Key Algorithms

**GSAP Parallax Pipeline** 🟢
```
ScrollTrigger(end: 600, scrub: 1)
  → 10 simultaneous .to() animations in a timeline
  → Each layer moves at different speeds/directions
  → Creates 3D depth illusion from 2D images
  → CTA button fades to opacity:0 at scroll end
```

**Responsive Decision** 🟢
- Decision point is in parent `gabriel.tsx`, not inside the component
- `isWide = windowProps.width && windowProps.height && windowProps.width > windowProps.height`
- Renders after Pace.js `done` event fires (page fully loaded)

### Business Rules
- Loading state persists until Pace.js fires `done` — hero is hidden (`hidden` class) while loading 🟢
- CTA button smoothly disappears as user scrolls — it is a soft nudge, not a gating mechanism 🟢

### Dependencies
- `gsap` (ScrollTrigger, ScrollToPlugin)
- `next/image` (with custom loader for basePath)
- `@/helpers/prefix` (basePath string)
- `@/lib/ga` (analytics events)

---

## Feature 2 — hero-dark

**Purpose:** Dark-themed profile section below the hero. Contains a 3-tab menu (Profile, Projects, Message) with animated content panels and a GSAP-driven sticky menu that expands on scroll.

### Primary Files
| File | Role |
|------|------|
| `src/components/views/dev/gabriel/HeroDark/index.tsx` | Container, tab state, profile photo carousel, sticky menu |
| `src/components/views/dev/gabriel/HeroDark/Jobs.tsx` | "Profile" tab — work history timeline + education |
| `src/components/views/dev/gabriel/HeroDark/Projects.tsx` | "Projects" tab — accordion project cards |
| `src/components/views/dev/gabriel/HeroDark/FuturePartner.jsx` | "Message" tab — recruiter-facing text (fully i18n) |

### Functions & Components

**`Herodark({ isWide: boolean | 0 | undefined })`** 🟢
- State: `selected: 'job' | 'projects' | 'partner'` (default: `'job'`)
- State: `loading: boolean` (menu transition lock)
- GSAP ScrollTrigger: profile photo crossfade carousel (3 sequential photos: gabriel-photo.png → gabriel-github.png → gabriel-photo.jpg)
- `onSelect(newSelected: string)`: sets loading, fades out `.should-hide` elements, scrolls to `#profile-photo`, then updates `selected` and fires GA event `menu_click` (category: newSelected)

**`Menu({ setSelected, selected, loading })`** 🟢
- GSAP ScrollTrigger: pins the menu, expands it to full viewport width (`window.innerWidth < 768 ? innerWidth : 850`), scales to 1.1, removes border-radius on scroll
- Three `Button` components (Profile, Projects, Message)

**`Button({ children, isActive, ...rest })`** 🟢
- Active state: gradient background (white→slate-300→gray-400), `cursor-not-allowed`, `disabled={true}`
- Inactive state: cursor-pointer, text-ternary, hover:text-figmaBlue

**`Jobs()`** 🟢
- Reads: `@/data/jobs.json` (5 entries), `@/data/stacks.json`, `@/data/swtools.json`
- State: `hasEducation: boolean` (default: true — shows education entries)
- Renders work history in reverse chronological order (lodash `_.reverse([...data])`)
- Hardcoded education entries inserted at index 3 and at the bottom (FATEC 2010-2013, UNINOVE 2008-2010)
- GA events: none directly in Jobs; menu click tracked in parent `onSelect`

**`Projects()`** 🟢
- Reads: `@/data/toshi-projects.json` (19 entries), `@/data/stacks.json`
- State: `expanded: string[]` — list of project `label` strings currently expanded
- `toggleAll()`: expands all or collapses all
- `toggleByLabel(label: string)`: adds/removes label from `expanded` array using `indexOf` + `splice`
- Renders in reverse chronological order (`_.reverse([...data])`)
- Expand animation via Tailwind `max-h-0` → `max-h-screen` transition

**`FuturePartner()`** 🟡
- Fully i18n via `useTranslation('future-partner')` namespace
- Uses `Trans` component from react-i18next for rich text (HTML elements within translations)
- `elemMapping`: maps `'strong'` → `<strong />`, `'a'` → `<a {...props} />`

### State Machine — Tab Selection
```
Initial: selected='job', loading=false

onSelect(newValue):
  loading = true
  GSAP: fade out .should-hide, scroll to #profile-photo
  on complete:
    selected = newValue
    loading = false
    GA.event('menu_click', { category: newValue })
```

### Business Rules
- Tab change is locked during animation (loading=true prevents rapid clicking) 🟢
- Projects are always displayed newest-first (reverse of JSON array order) 🟢
- Education is shown by default but can be hidden 🟢
- `label` field in projects.json acts as expand/collapse key — **duplicate labels exist** (`"requirement"` appears 5 times) 🔴 **BUG:** toggling one "requirement" project expands all with same label simultaneously

### ToshiAITerminal (sub-feature de hero-dark) 🟢

**Arquivos:**
| File | Role |
|------|------|
| `src/components/views/dev/gabriel/HeroDark/ToshiAITerminal/index.tsx` | Terminal UI — input, streaming output, char counter, question limit |
| `src/components/views/dev/gabriel/HeroDark/ToshiAITerminal/useOpenRouterStream.ts` | SSE client → Cloudflare Worker |
| `src/components/views/dev/gabriel/HeroDark/ToshiAITerminal/MarkdownContent.tsx` | Renderiza respostas com react-markdown + syntax highlighting |

**`ToshiAITerminal()`** 🟢
- Constantes: `MAX_QUESTIONS = 3`, `MAX_CHARS = 200`
- Renderização condicional: `if (!workerUrl) return null` — guard via `NEXT_PUBLIC_TOSHI_AI_WORKER_URL`
- Estado: `messages: Message[]`, `questionsUsed: number`, `isStreaming: boolean`, `input: string`, `isFocused: boolean`
- Refs: `outputRef` (auto-scroll), `inputRef` (auto-focus pós-streaming), `prevStreamingRef` (detecta fim de streaming)
- Após cada resposta completa: GA event `ai_terminal_query` com label = questões restantes
- Blinking cursor: classe CSS `.cursor-blink` no `styles/globals.css` (`@keyframes blink`)
- Todos os textos são `font-mono`; largura máxima `max-w-3xl`

**`sendMessage(question, onChunk)`** em `useOpenRouterStream.ts` 🟢
- POST para `NEXT_PUBLIC_TOSHI_AI_WORKER_URL` com body `{ question }` — sem header Authorization
- Parser SSE linha a linha: detecta `data: <json>` e `data: [DONE]`
- Extrai `data.model` e `data.usage.total_tokens` do último chunk
- Retorna `{ model: string, totalTokens: number }`

**`MarkdownContent({ content })`** 🟢
- Usa `react-markdown` com plugins `remark-gfm` e `rehype-raw`
- Code blocks renderizados com `react-syntax-highlighter` (tema `dracula`)

### Business Rules — ToshiAI
- Máximo 3 perguntas por sessão (client-side, sem enforcement server-side) 🟢
- Máximo 200 caracteres por pergunta 🟢
- Input desabilitado durante streaming ou ao atingir limite 🟢
- Erro de rede desconta a pergunta da contagem (rollback) 🟢
- Model name e total de tokens exibidos abaixo de cada resposta completa 🟢

### Dependencies
- `gsap`, `dayjs`, `lodash`, `react-i18next`, `next/image`
- `react-markdown`, `react-syntax-highlighter` (usados pelo ToshiAITerminal)
- `@/data/jobs.json`, `@/data/stacks.json`, `@/data/swtools.json`, `@/data/toshi-projects.json`

---

## Feature 3 — main-content

**Purpose:** Long-form editorial article titled "The 3-3-3 Principles for a Better UX". Static text with illustrative screenshot images. Positioned below `hero-dark`.

### Primary Files
| File | Role |
|------|------|
| `src/components/views/dev/gabriel/MainContent/MainContent.tsx` | Layout wrapper |
| `src/components/views/dev/gabriel/MainContent/Introduction.tsx` | Full article content (static JSX) |
| `src/components/views/dev/gabriel/MainContent/index.ts` | Barrel export |

### Functions & Components

**`MainContent()`** 🟢
- Thin wrapper: renders `<main>` with max-width + typography classes, wraps `Introduction`

**`Introduction()`** 🟢
- Fully static — no props, no state, no hooks
- Renders the "3-3-3 Principles" article in JSX prose
- 4 screenshot images referenced: `ss-netflix.png`, `ss-maple.png`, `ss-twitter.png`, `ss-uber.png`
- Article structure:
  - 3 Communication Goals: (1) Cause Reflection, (2) Make Decision, (3) Make Action
  - 3 Basic Design Principles: (1) Evoking Emotions, (2) AIA Composition, (3) 60-30-10 Color Proportion
  - 3 Life Principles: (1) Everything Changes, (2) Simplicity is Key, (3) The Hypnotic Gift

### Business Rules
- Entire article is hardcoded in English only — not internationalized 🟡 INFERRED (no `useTranslation` hook)
- Bottom of article contains placeholder social links: "Let's connect", "Share this page", "Buy me a Coffee" — **not functional** (plain text spans, no hrefs) 🟢

---

## Feature 4 — analytics

**Purpose:** Google Analytics 4 integration, production-only, with SPA route-change tracking and custom event firing.

### Primary Files
| File | Role |
|------|------|
| `src/components/dom/Analytics/index.tsx` | Production gate |
| `src/components/dom/Analytics/GoogleAnalytics.tsx` | GA script loader + router event listener |
| `src/lib/ga.ts` | Utility functions: pageview, event |
| `src/components/dom/Analytics/Plausible.js` | Alternate provider (commented out) |
| `src/components/dom/Analytics/SimpleAnalytics.js` | Alternate provider (commented out) |

### Functions

**`Analytics()`** 🟢
- Renders `<GA />` only when `process.env.NODE_ENV === 'production'`
- Plausible and SimpleAnalytics integrations present but fully commented out

**`GAScript()`** 🟢
- Next.js `<Script strategy='lazyOnload'>` — non-blocking load
- Subscribes to `router.events.on('routeChangeComplete', handleRouteChange)` on mount
- Unsubscribes on unmount (no memory leak)
- `handleRouteChange(url)`: calls `ga.pageview(url)`

**`pageview(url: string)`** 🟢
- Calls `window.gtag('config', GA_ID, { page_path: url })` — guarded by `window && window.gtag`

**`event({ action, params })`** 🟢
- Calls `window.gtag('event', action, params)` — guarded by `window && window.gtag`

**`hasGa()`** 🟡
- Returns `window && window.gtag` — name implies boolean check but return type is `void` in signature, actual return is the expression value

### GA Events Fired
| Event | Location | Parameters |
|-------|----------|-----------|
| `menu_click` | `HeroDark/index.tsx:onSelect` | `event_category: 'job'|'projects'|'partner'` |
| `hero_cta` | `HeroSection/WideScreen.tsx:handleAction` | `event_category: '3-3-3 Principle'`, `event_label: 'widescreen'` |
| `hero_cta` | `HeroSection/Mobile.tsx:handleAction` | `event_category: '3-3-3 Principle'`, `event_label: 'mobile screen'` |

### Business Rules
- GA is completely disabled in development — no tracking, no console noise 🟢
- GA measurement ID is injected at build-time via env var, not hardcoded in source 🟢
- GA ID: `G-RH74DXLKZL` (set in CI/CD workflow, not in source code) 🟢

---

## Feature 5 — i18n

**Purpose:** Multi-language support for 3 locales (English, Japanese, Brazilian Portuguese) via route-based locale switching and browser language auto-detection.

### Primary Files
| File | Role |
|------|------|
| `next-i18next.config.js` | Root i18n config — locales list, default locale |
| `src/lib/getStatic.js` | Static path/prop generators for [locale] pages |
| `src/lib/languageDetector.js` | Browser language detection config |
| `src/lib/redirect.js` | Client-side locale redirect logic |
| `public/locales/en/common.json` | EN translations |
| `public/locales/ja/common.json` | JA translations |
| `public/locales/pt-BR/common.json` | PT-BR translations |
| `public/locales/*/future-partner.json` | Recruiter section translations (3 locales) |

### Functions

**`getI18nPaths()`** 🟢
- Returns `[{ params: { locale: 'en' } }, { params: { locale: 'ja' } }, { params: { locale: 'pt-BR' } }]`
- Used as `getStaticPaths` return for `[locale]` pages

**`makeStaticProps(ns)`** 🟢
- HOF returning `getStaticProps` that calls `serverSideTranslations(locale, ns)`
- Default namespaces: `['common', 'future-partner']`

**`useRedirect(to?)`** 🟢
- Detects browser language via `languageDetector.detect()`
- Computes basePath: `/fullstack-profile/` in production, `/` in dev
- Guards against redirect loops: if current route is `/404` and target starts with `basePath + detectedLng`, redirects to `basePath + detectedLng + route`
- Caches detected language via `languageDetector.cache(detectedLng)`
- Calls `router.replace(basePath + detectedLng)` on every render (runs as effect) 🔴 **POTENTIAL BUG:** no dependency array → runs on every render, not just once on mount

**`Redirect`** / **`getRedirect(to)`** 🟢
- Thin wrappers around `useRedirect` for use as page components

### Route Structure
```
/                          → redirects to /<detected-locale>
/<locale>/                 → redirects to /<locale>/dev/gabriel-toshinori-nakano/
/<locale>/dev/gabriel/     → legacy alias, redirects to gabriel-toshinori-nakano
/<locale>/dev/gabriel-toshinori-nakano/  → main portfolio page
```

### Supported Locale Keys (common.json)
See `data-dictionary.md` for full translation key schema.

### Business Rules
- Default locale is `en` — English is the fallback for unknown browser languages 🟢
- Locale is part of the URL path, not a query param or cookie — SEO-friendly 🟢
- Article content (Introduction.tsx) is **not translated** — always renders in English regardless of locale 🟡 INFERRED
- Language switcher (ChangeLanguage) is always visible, fixed top-right 🟢

---

## Feature 6 — layout

**Purpose:** Public page shell providing consistent `<Head>` meta, Unicons icon CDN link, and the ChangeLanguage widget.

### Primary Files
| File | Role |
|------|------|
| `src/components/layout/Public/index.tsx` | Layout wrapper |
| `src/components/dom/ChangeLanguage/index.tsx` | Fixed language switcher |

### Functions

**`Public({ children, title })`** 🟢
- Sets `<title>` tag to passed `title` prop
- Loads Unicons icon set from CDN: `https://unicons.iconscout.com/release/v4.0.0/css/line.css`
- Renders ChangeLanguage widget
- Favicon links, webmanifest, theme-color meta

**`ChangeLanguage()`** 🟢
- Fixed position, z-[1000] (above all content)
- Three buttons: English → `router.push(prefix + '/en')`, Japanese → `router.push(prefix + '/ja')`, Portuguese → `router.push(prefix + '/pt-BR')`
- `prefix` = `NEXT_PUBLIC_BASE_PATH` env var (resolves to `/fullstack-profile` in production)

### Business Rules
- Unicons icon set is loaded from CDN on every page — required for `uil-*` icon classes used in Jobs and Projects 🟢
- Language buttons perform hard navigation (full page redirect), not seamless SPA locale switch 🟡 INFERRED (router.push with full path)

---

## Feature 7 — data

**Purpose:** Static JSON data layer — no API, no database. All content is embedded at build time.

### Primary Files
| File | Role |
|------|------|
| `src/data/jobs.json` | 5 professional work history entries |
| `src/data/toshi-projects.json` | 19 portfolio projects spanning 2011-2025 |
| `src/data/stacks.json` | Tech stack icon/metadata registry |
| `src/data/swtools.json` | Software tools icon/metadata registry |
| `src/helpers/prefix.ts` | `NEXT_PUBLIC_BASE_PATH` constant |
| `src/helpers/customLoader.ts` | Next.js Image custom loader (identity function) |

### Data Schemas
See `data-dictionary.md` for complete field-level documentation.

### Key Constants & Configuration
| Constant | Source | Value |
|----------|--------|-------|
| `prefix` | `NEXT_PUBLIC_BASE_PATH` env var | `/fullstack-profile` (prod) / `''` (dev) |
| `customLoader` | `src/helpers/customLoader.ts` | Identity function — returns `src` as-is |
| GA ID | `NEXT_PUBLIC_GOOGLE_ANALYTICS` env var | `G-RH74DXLKZL` |

### Business Rules
- `customLoader` is an identity function — it bypasses Next.js image optimization entirely 🟢
- This is required for GitHub Pages static hosting (no server-side image optimization) 🟢
- All images use `unoptimized` prop in addition to custom loader — belt-and-suspenders approach 🟢

---

## Feature 8 — worker (Cloudflare Worker AI Proxy)

> **NOVO** — Adicionado em 2026-05-17 (feature 005-cloudflare-worker-ai-proxy)

**Purpose:** Edge proxy que isola a chave OpenRouter do frontend. Recebe `{ question }` do browser, constrói o system prompt dinamicamente, e encaminha o stream SSE do OpenRouter de volta ao cliente.

### Primary Files
| File | Role |
|------|------|
| `worker/src/index.ts` | HTTP handler — CORS, routing POST/OPTIONS, proxy SSE |
| `worker/src/systemPrompt.ts` | Construção do system prompt a partir dos JSONs de dados |
| `worker/wrangler.toml` | Config Cloudflare: `name = "fullstack-profile-ai"` |
| `worker/package.json` | Dependências do sub-projeto (wrangler ^3, @cloudflare/workers-types ^4) |
| `worker/.dev.vars.example` | Template de secrets para `wrangler dev` local |

### Functions

**`fetch(request, env)`** em `worker/src/index.ts` 🟢
- `env.OPENROUTER_API_KEY` — chave do OpenRouter (Cloudflare secret, nunca no frontend)
- `env.OPENROUTER_MODEL` — modelo a usar (Cloudflare secret)
- CORS: `ALLOWED_ORIGIN = 'https://gtoshinakano.github.io'`
- `OPTIONS` → 204 com headers `Access-Control-Allow-*` (preflight)
- `POST` com Origin inválida → 403
- `POST` com Origin válida → parse `{ question }`, chama `buildSystemPrompt()`, POST OpenRouter, pipe `upstream.body` como `text/event-stream`
- Erros: 400 (bad input), 502 (upstream error), 504 (timeout), todos com headers CORS

**`buildSystemPrompt()`** em `worker/src/systemPrompt.ts` 🟢
- Importa staticamente `../../src/data/jobs.json`, `toshi-projects.json`, `stacks.json`, `swtools.json`
- Os JSONs são bundled pelo esbuild em tempo de deploy — fonte de verdade compartilhada com o frontend
- `serializeJobs()`: formata histórico de trabalho (empresa | cargo | período | stacks)
- `serializeProjects()`: formata projetos em ordem reversa (mais recente primeiro)
- `collectSkills()`: unique set de stacks/tools de todos os jobs + projetos, resolvidos pelos registries

### CORS Policy 🟢
```
ALLOWED_ORIGIN = 'https://gtoshinakano.github.io'
Toda request com Origin diferente → 403 imediato
OPTIONS (preflight) com Origin inválida → 403 (não 204)
```

### SSE Streaming 🟢
```
Worker recebe stream do OpenRouter → pipe direto (upstream.body)
Sem buffering. Content-Type: text/event-stream, Cache-Control: no-cache
Frontend parser identifica: data: <json> e data: [DONE]
```

### Business Rules — Worker
- API key nunca sai do worker (é Cloudflare secret injetado em runtime) 🟢
- System prompt gerado server-side — conteúdo nunca trafega na rede 🟢
- CORS restrito a um único domínio de produção — sem allowlist dinâmica 🟢
- Sem rate limiting server-side (limite de 3 perguntas é client-only) 🟡 INFERRED

### Deployment
- `cloudflare/wrangler-action@v3` no `.github/workflows/deploy.yml`
- Secrets `OPENROUTER_API_KEY` e `OPENROUTER_MODEL` injetados via `secrets:` block do wrangler action
- Deploy ocorre ANTES do build do frontend em cada pipeline run

---

## Cross-Cutting Concerns

### Loading Sequence
```
1. Browser loads /fullstack-profile (GitHub Pages)
2. Next.js static page loads, client-side JS executes
3. useRedirect() fires → router.replace to /fullstack-profile/<locale>/dev/...
4. gabriel.tsx mounts → loading=true
5. Pace.js CDN script loads → monitors resource loading
6. Pace.js fires 'done' → loading=false, windowProps set
7. isWide computed → HeroSection.WideScreen or Mobile renders
8. GSAP animations initialize
9. GA lazyOnload scripts fire
```

### Known Issues / Gaps
| Issue | Severity | Location |
|-------|----------|----------|
| Duplicate `label:"requirement"` in toshi-projects.json — 5 projects share same key | 🔴 BUG | `src/data/toshi-projects.json` |
| `useRedirect` runs on every render (no deps array) | 🔴 POTENTIAL BUG | `src/lib/redirect.js:12` |
| Introduction article not internationalized | 🟡 INFERRED GAP | `src/components/views/dev/gabriel/MainContent/Introduction.tsx` |
| Social links at bottom of article not functional (plain text spans) | 🟡 INFERRED | `Introduction.tsx:621` |
| `hasGa()` return type declared `void` but returns expression | 🟡 INFERRED | `src/lib/ga.ts:28` |
| Unicons, Pace.js loaded from CDN — no version pinning | 🟡 INFERRED | `layout/Public/index.tsx`, `pages/_app.tsx` |
| `@headlessui/react`, `recharts` declared but not found in active components | 🟡 INFERRED | `package.json` |
| `react-markdown`, `react-syntax-highlighter` — **agora ativos** em `ToshiAITerminal/MarkdownContent.tsx` | 🟢 RESOLVED | `package.json` |
