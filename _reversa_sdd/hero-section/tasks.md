# Hero Section — Tarefas de Implementação

> Template do arquivo `tasks.md`. Sequência executável para reimplementar a unit a partir do legado.
> Generated by Reversa Writer · 2026-05-17

## Pré-requisitos

- [ ] GSAP 3.x instalado (`gsap` com plugins `ScrollTrigger` e `ScrollToPlugin`)
- [ ] Pace.js disponível via `<script>` no `<head>` (CDN ou bundle local)
- [ ] `NEXT_PUBLIC_BASE_PATH` configurado no ambiente de build
- [ ] `customLoader.ts` implementado (ver feature `layout`)
- [ ] `prefix.ts` disponível (ver feature `layout`)
- [ ] Assets de imagem das camadas montanha em `public/img/` (10 PNGs: camadas 1–10)
- [ ] Estado `loading` e `windowProps` gerenciados pelo componente pai (`gabriel.tsx`)

## Tarefas

- [ ] T-01 — Implementar a lógica de gate do Pace.js em `gabriel.tsx`
  - Origem no legado: `pages/[locale]/dev/gabriel-toshinori-nakano/gabriel.tsx:25-45`
  - Critério de pronto: `loading` inicia `true`; ao disparar `Pace.on('done', cb)`, `loading` vai a `false` e `windowProps` é populado com `window.innerWidth` e `window.innerHeight`
  - Confiança: 🟢

- [ ] T-02 — Implementar cálculo de `isWide` por aspect ratio
  - Origem no legado: `gabriel.tsx:37-40`
  - Critério de pronto: `isWide = windowProps.width > windowProps.height`; redimensionar o viewport recalcula o valor via listener de `resize`
  - Confiança: 🟢

- [ ] T-03 — Criar componente `HeroSvg` com texto SVG inline
  - Origem no legado: `src/components/dom/HeroSection/HeroSvg.js`
  - Critério de pronto: SVG "FULL STACK DEV. / developer.p" renderizado sobre o hero em posição absoluta com z-index superior às camadas
  - Confiança: 🟢

- [ ] T-04 — Implementar componente `Mobile` (hero para portrait)
  - Origem no legado: `src/components/dom/HeroSection/Mobile.tsx`
  - Critério de pronto: renderiza imagem de fundo estática via `<Image loader={customLoader}>` + `HeroSvg` + botão CTA; sem GSAP
  - Confiança: 🟢

- [ ] T-05 — Implementar as 10 camadas de imagem no componente `WideScreen`
  - Origem no legado: `src/components/dom/HeroSection/WideScreen.tsx`
  - Critério de pronto: 10 `<Image>` empilhadas via CSS `position: absolute` com z-index sequencial; cada camada tem `id` para referência no GSAP
  - Confiança: 🟢

- [ ] T-06 — Criar `useEffect([loading])` com `gsap.set()` e timeline de paralaxe em `WideScreen`
  - Origem no legado: `WideScreen.tsx:17-107` — `useEffect` (não useLayoutEffect) com guard `if (!loading)`
  - Critério de pronto: quando `loading=false`, gsap.set() posiciona todas as camadas; timeline com `scrub:1`, `end: 600` (sem trigger explícito — usa trigger padrão do viewport); cada camada recebe tween de translateY/rotateY/zoom distintos
  - Confiança: 🟢
  - Confiança: 🟢

- [ ] T-07 — Implementar animações de entrada dos elementos textuais (título, subtítulo, CTA)
  - Origem no legado: `WideScreen.tsx` — tweens escalonados com `stagger` e `delay`
  - Critério de pronto: elementos aparecem com fade/slide no carregamento após `loading=false`; ordem: título → subtítulo → CTA
  - Confiança: 🟡

- [ ] T-08 — Implementar CTA button com `scrollTo` via GSAP
  - Origem no legado: `WideScreen.tsx:109-115` e `Mobile.tsx:116` — `gsap.to(window, { scrollTo: 'main', duration: 0.5 })`
  - Critério de pronto: clicar no botão rola suavemente até o elemento `<main>` da página; GA event chamado com `ga.event({ action: 'hero_cta', params: { event_category: '3-3-3 Principle', event_label: 'widescreen' } })` (ou `'mobile screen'`)
  - Confiança: 🟢

- [ ] T-09 — Garantir cleanup do GSAP ao desmontar / alternar entre WideScreen e Mobile
  - Origem no legado: ausência de cleanup identificada — gap no legado 🔴
  - Critério de pronto: `ScrollTrigger.getAll().forEach(t => t.kill())` chamado no return do `useLayoutEffect`
  - Confiança: 🔴

## Tarefas de Teste

- [ ] TT-01 — Happy path WideScreen: carregar em viewport landscape → hero aparece com paralaxe funcional após Pace `done`
- [ ] TT-02 — Happy path Mobile: carregar em viewport portrait → hero estático aparece sem GSAP
- [ ] TT-03 — CTA scroll: clicar no botão → página rola até `#article` e GA event é disparado
- [ ] TT-04 — Resize: rotacionar viewport de landscape para portrait → componente alterna de WideScreen para Mobile sem erro
- [ ] TT-05 — Gate de loading: simular Pace `done` atrasado → hero permanece oculto durante a espera

## Ordem Sugerida

1. T-01 e T-02 primeiro — o gate e o cálculo de `isWide` são a base de toda a renderização condicional
2. T-03 (HeroSvg) — componente sem dependências, pode ser feito isolado
3. T-04 (Mobile) — mais simples, valida o fluxo completo sem GSAP
4. T-05 → T-06 → T-07 (WideScreen, camadas, paralaxe, animações de entrada) — sequência dependente
5. T-08 (CTA) — depende de T-04 e T-06 para estar em contexto real
6. T-09 (cleanup) — sempre por último, após verificar vazamentos com Chrome DevTools

- [ ] T-10 — Implementar timeout de fallback de 5 segundos para o gate do Pace.js
  - Decisão do autor: sim, 5 segundos (✅ Respondida — Pergunta 1)
  - Origem no legado: gap — não implementado
  - Critério de pronto: em `gabriel.tsx`, após registrar `Pace.on('done', cb)`, iniciar `setTimeout(() => setLoading(false), 5000)` que força `loading=false` caso o evento `done` nunca chegue; o timeout deve ser cancelado se o evento `done` chegar antes (usar `clearTimeout`)
  - Confiança: 🟢

## Lacunas Pendentes (🔴)

- **T-09** — O legado não implementa cleanup do GSAP ScrollTrigger ao alternar de WideScreen para Mobile. A reimplementação deve corrigir este comportamento; estratégia recomendada: `gsap.context().revert()` no return do `useEffect`.
