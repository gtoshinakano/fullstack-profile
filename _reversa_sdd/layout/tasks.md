# Layout — Tarefas de Implementação

> Template do arquivo `tasks.md`. Sequência executável para reimplementar a unit a partir do legado.
> Generated by Reversa Writer · 2026-05-17

## Pré-requisitos

- [ ] `NEXT_PUBLIC_BASE_PATH=/fullstack-profile` configurado no ambiente de produção
- [ ] Conta Google Fonts (sem autenticação — apenas URL pública)
- [ ] URLs CDN do Pace.js e Unicons disponíveis e testadas

## Tarefas

- [ ] T-01 — Implementar `src/helpers/prefix.ts`
  - Origem no legado: `src/helpers/prefix.ts`
  - Critério de pronto: `const prefix = process.env.NEXT_PUBLIC_BASE_PATH ?? ''`; exportado como default; retorna `'/fullstack-profile'` em produção e `''` em dev
  - Confiança: 🟢

- [ ] T-02 — Implementar `src/helpers/customLoader.ts`
  - Origem no legado: `src/helpers/customLoader.ts`
  - Critério de pronto: `const customLoader = ({ src }: { src: string }) => src`; exportado como default; configurado em `next.config.js` como `images.loaderFile`
  - Confiança: 🟢

- [ ] T-03 — Configurar `next.config.js` com `assetPrefix`, `output: 'export'` e `images.loaderFile`
  - Origem no legado: `next.config.js`
  - Critério de pronto: `output: 'export'`; `assetPrefix: isProd ? '/fullstack-profile/' : ''`; `images: { loader: 'custom', loaderFile: './src/helpers/customLoader.ts' }`; build conclui sem erro
  - Confiança: 🟢

- [ ] T-04a — Hospedar Pace.js localmente em `public/pace.min.js`
  - Decisão do autor (Pergunta 8): hospedar localmente ✅
  - Origem no legado: `Public/index.tsx` — `<script>` apontando para CDN externo sem versão
  - Critério de pronto: baixar `pace.min.js` da versão estável atual do Pace.js; salvar em `public/pace.min.js`; URL no `<script>` atualizada para `${prefix}/pace.min.js` (com basePath)
  - Confiança: 🟢

- [ ] T-04 — Criar componente `layout/Public` com injeção de head e CDN
  - Origem no legado: `src/components/layout/Public/index.tsx`
  - Critério de pronto: `<Head>` com title, description, viewport, OG tags; `<link>` Google Fonts Syne; `<link>` Unicons CDN; `<script src="${prefix}/pace.min.js">` (local — ver T-04a); `{children}` renderizados após o head
  - Confiança: 🟢

- [ ] T-05 — Implementar widget `ChangeLanguage`
  - Origem no legado: `src/components/dom/ChangeLanguage/index.tsx`
  - Critério de pronto: exibe EN / 日本語 / PT-BR; locale ativo destacado visualmente; ao clicar: `router.push(prefix + '/' + locale + '/dev/gabriel-toshinori-nakano/')`; sem erro em dev (prefix vazio) nem em produção (prefix com basePath)
  - Confiança: 🟢

- [ ] T-06 — Integrar `Public` como layout em `gabriel.tsx`
  - Origem no legado: `gabriel.tsx` — `<Public>` envolvendo todo o conteúdo da página
  - Critério de pronto: `<Public title="..." description="...">` é o elemento raiz de `gabriel.tsx`; `ChangeLanguage` aparece na página sem configuração adicional
  - Confiança: 🟢

- [ ] T-07 — Criar `.env.example` documentando variáveis necessárias
  - Origem no legado: ausente — gap identificado
  - Critério de pronto: arquivo `.env.example` na raiz com `NEXT_PUBLIC_BASE_PATH=/fullstack-profile` e `NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX` comentados e explicados
  - Confiança: 🔴 (gap do legado — não existe no legado)

## Tarefas de Teste

- [ ] TT-01 — `prefix.ts`: em dev, retorna `''`; com `NEXT_PUBLIC_BASE_PATH=/fullstack-profile`, retorna `/fullstack-profile`
- [ ] TT-02 — `customLoader`: `customLoader({ src: '/img/test.png' })` retorna `'/img/test.png'` sem modificação
- [ ] TT-03 — Build: `npm run build` conclui sem erro de image optimizer
- [ ] TT-04 — Assets em produção: imagens carregam em `gtoshinakano.github.io/fullstack-profile/img/...`
- [ ] TT-05 — Pace.js: `window.Pace` está definido após carregamento da página
- [ ] TT-06 — ChangeLanguage: trocar idioma em produção resulta em URL com `/fullstack-profile/` no path

## Ordem Sugerida

1. T-01 (`prefix.ts`) e T-02 (`customLoader.ts`) — helpers sem dependências, base de tudo
2. T-03 (`next.config.js`) — configuração de build; necessária para qualquer teste de build
3. T-04 (`Public` layout) — shell de página; sem ele nenhuma página tem head correto
4. T-05 (`ChangeLanguage`) — depende de T-01 para o prefix
5. T-06 (integração em `gabriel.tsx`) — une tudo
6. T-07 (`.env.example`) — documentação; pode ser feito a qualquer momento

## Lacunas Pendentes (🔴)

- **T-07 (`.env.example` ausente):** o legado não documenta as variáveis de ambiente necessárias. A reimplementação deve criar este arquivo para onboarding correto de novos desenvolvedores.
- **Fallback do Pace.js:** nenhum timeout implementado no legado. Decisão pendente: adicionar `setTimeout(() => setLoading(false), 5000)` em `gabriel.tsx` como fallback caso o Pace CDN não responda?
- **Pinagem de versão do Pace.js:** a URL CDN não inclui versão. Decisão pendente: hospedar o script em `public/pace.min.js` para eliminar dependência de CDN externo?
