# Analytics — Tarefas de Implementação

> Template do arquivo `tasks.md`. Sequência executável para reimplementar a unit a partir do legado.
> Generated by Reversa Writer · 2026-05-17

## Pré-requisitos

- [ ] `NEXT_PUBLIC_GA_ID` configurado no ambiente de produção (GitHub Actions secrets ou `.env.production`)
- [ ] `next/script` disponível (Next.js 11+)
- [ ] `next/router` disponível para `router.events`

## Tarefas

- [ ] T-01 — Implementar helpers `pageview` e `event` em `src/lib/ga.ts`
  - Origem no legado: `src/lib/ga.ts`
  - Critério de pronto: `pageview(url)` chama `window.gtag('config', NEXT_PUBLIC_GOOGLE_ANALYTICS, { page_path: url })`; `event({ action, params })` onde `params: { event_category?, event_label? }` chama `window.gtag('event', action, params)`; guard `if (window && window.gtag)` antes de qualquer chamada
  - Confiança: 🟢

- [ ] T-02 — Criar componente `GAScript` com injeção de script e listener de rota
  - Origem no legado: `src/components/dom/Analytics/GoogleAnalytics.tsx`
  - Critério de pronto: dois `<Script strategy="lazyOnload">` injetados (gtag.js CDN + script inline de inicialização); `useEffect([router.events])` com `router.events.on('routeChangeComplete', handleRouteChange)`; cleanup com `router.events.off()` no return do `useEffect` (cleanup JÁ implementado no legado)
  - Confiança: 🟢

- [ ] T-03 — Criar componente `Analytics` com gate de produção
  - Origem no legado: `src/components/dom/Analytics/index.tsx:7`
  - Critério de pronto: `if (process.env.NODE_ENV !== 'production') return null`; caso contrário renderiza `<GoogleAnalytics />`; componente montado em `gabriel.tsx` fora do condicional `loading`
  - Confiança: 🟢

- [ ] T-04 — Integrar `ga.event('menu_click')` em `HeroDark`
  - Origem no legado: `HeroDark/index.tsx` — callback de conclusão do GSAP
  - Critério de pronto: ao concluir a transição de aba, `ga.event({ action: 'menu_click', category: tab })` é chamado; verificar que a chamada ocorre **após** `loading=false` (não durante a animação)
  - Confiança: 🟢

- [ ] T-05 — Integrar `ga.event('hero_cta')` em HeroSection
  - Origem no legado: `WideScreen.tsx` e `Mobile.tsx` — handler do CTA button
  - Critério de pronto: ao clicar no botão CTA, `ga.event({ action: 'hero_cta', category: '3-3-3 Principle', label: 'widescreen' })` (ou `'mobile screen'`) é disparado antes do `scrollTo`
  - Confiança: 🟢

## Tarefas de Teste

- [ ] TT-01 — Gate de produção: em `NODE_ENV=development`, verificar que nenhum `<script>` gtag aparece no DOM e que `ga.event()` não lança exceção
- [ ] TT-02 — Guard de gtag: em ambiente sem `window.gtag`, chamar `ga.pageview('/test')` e `ga.event({...})` → sem throw, sem log de erro
- [ ] TT-03 — Pageview: em produção (ou com `NODE_ENV=production` local), trocar locale → verificar hit no GA4 Real-Time
- [ ] TT-04 — `menu_click`: trocar aba em produção → verificar evento no GA4 Real-Time com parâmetros corretos
- [ ] TT-05 — Cleanup: verificar que `router.events.off()` está no return do `useEffect` para evitar listener duplicado

## Ordem Sugerida

1. T-01 (helpers `ga.ts`) — base usada por todas as outras tarefas e features
2. T-02 (GoogleAnalytics) — componente de infraestrutura
3. T-03 (gate Analytics) — wrapper final; pode ser testado localmente com `NODE_ENV=production`
4. T-04 e T-05 (integrações em HeroDark e HeroSection) — apenas após T-01 estar estável

## Lacunas Pendentes (🔴)

- **`NEXT_PUBLIC_GOOGLE_ANALYTICS` não documentado:** o legado não tem `.env.example`. A reimplementação deve criar `.env.example` com `NEXT_PUBLIC_GOOGLE_ANALYTICS=G-XXXXXXXXXX` para onboarding correto.
- **Modo debug em dev:** considerar `console.debug('[GA]', action, params)` em `ga.ts` quando `NODE_ENV !== 'production'`, para facilitar validação de eventos sem deploy em produção. Decisão de implementação pendente.
