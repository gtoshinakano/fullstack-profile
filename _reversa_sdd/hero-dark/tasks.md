# Hero Dark — Tarefas de Implementação

> Template do arquivo `tasks.md`. Sequência executável para reimplementar a unit a partir do legado.
> Generated by Reversa Writer · 2026-05-17

## Pré-requisitos

- [ ] GSAP 3.x instalado com plugins `ScrollTrigger` e `ScrollToPlugin`
- [ ] `lodash` disponível (`_.reverse`)
- [ ] `dayjs` disponível para formatação de datas
- [ ] `next-i18next` configurado (ver feature `i18n`)
- [ ] `src/lib/ga.ts` implementado (ver feature `analytics`)
- [ ] `src/data/jobs.json` e `src/data/toshi-projects.json` disponíveis
- [ ] Assets de fotos do autor em `public/img/` (`gabriel-photo.png`, `gabriel-github.png`, `gabriel-photo.jpg`)

## Tarefas — Orquestrador (`HeroDark/index.tsx`)

- [ ] T-01 — Implementar máquina de estado de abas com `useState`
  - Origem no legado: `HeroDark/index.tsx` — `useState('job')` e `useState(false)` para loading
  - Critério de pronto: `selected` inicia em `'job'`; `loading` inicia em `false`
  - Confiança: 🟢

- [ ] T-02 — Implementar `onSelect(tab)` com trava de loading e transição GSAP
  - Origem no legado: `HeroDark/index.tsx:50-68`
  - Critério de pronto: ao chamar `onSelect`, `loading` vai a `true`; GSAP anima saída da aba atual (stagger 0.05 × n elementos); `scrollTo` volta ao topo da seção; aba nova é exibida com fade in; `loading` retorna a `false`; botões permanecem desabilitados durante todo o processo
  - Confiança: 🟢

- [ ] T-03 — Disparar GA event `menu_click` ao concluir transição
  - Origem no legado: `HeroDark/index.tsx` — callback de conclusão do GSAP
  - Critério de pronto: `ga.event({ action: 'menu_click', category: tab })` chamado apenas após animação completa
  - Confiança: 🟢

- [ ] T-04 — Implementar carrossel de 3 fotos via GSAP ScrollTrigger
  - Origem no legado: `HeroDark/index.tsx` — `useLayoutEffect` com ScrollTrigger em `#profile-photo`
  - Critério de pronto: `scrub:1`, `start:'top center'`, `end:'10% 10%'`; foto 1 → 2 → 3 com fade conforme scroll; reversível ao scrollar para cima
  - Confiança: 🟢

- [ ] T-05 — Implementar expansão do menu lateral ao entrar na seção (sticky)
  - Origem no legado: `HeroDark/index.tsx` — ScrollTrigger com `toggleClass` ou `onEnter`
  - Critério de pronto: ao rolar até `#hero-dark`, o menu lateral expande visualmente via classe CSS; ao sair, contrai
  - Confiança: 🟡

- [ ] T-06 — Renderizar condicionalmente `Jobs`, `Projects` ou `FuturePartner` conforme `selected`
  - Origem no legado: `HeroDark/index.tsx` — JSX condicional por `selected`
  - Critério de pronto: apenas o componente da aba ativa está montado (ou visível); troca ocorre após animação GSAP
  - Confiança: 🟢

## Tarefas — Componente Jobs

- [ ] T-07 — Carregar `jobs.json`, inverter com `_.reverse` e renderizar timeline
  - Origem no legado: `Jobs.tsx:35` — `_.reverse([...data])`
  - Critério de pronto: job mais recente aparece no topo; datas formatadas via `dayjs`; jobs em andamento (`ongoing: true`) exibem "Present" no lugar da data de fim
  - Confiança: 🟢

- [ ] T-08 — Adicionar entradas de educação hardcoded no JSX
  - Origem no legado: `Jobs.tsx:90-113` (FATEC, posição 3) e `Jobs.tsx:195-225` (UNINOVE, última posição)
  - Critério de pronto: FATEC 2010–2013 aparece na terceira posição da timeline; UNINOVE 2008–2010 aparece no final; ambas exibidas quando `hasEducation=true`
  - Confiança: 🟢

- [ ] T-09 — Implementar toggle de educação com botão "Hide/Show Degrees"
  - Origem no legado: `Jobs.tsx:19` — `hasEducation` state; botão alterna entre "Hide Degrees" e "Show Degrees"
  - Critério de pronto: ao clicar, as duas entradas de educação somem/reaparecem; label do botão reflete o estado atual; `hasEducation` inicia em `true`
  - Confiança: 🟢

## Tarefas — Componente Projects

- [ ] T-10 — Carregar `toshi-projects.json`, inverter e renderizar lista de projetos
  - Origem no legado: `Projects.tsx:72` — `_.reverse([...data])`
  - Critério de pronto: projeto mais recente no topo; cada item exibe título, categoria, período e role
  - Confiança: 🟢

- [ ] T-11 — Implementar accordion com `expanded[]` state
  - Origem no legado: `Projects.tsx` — `useState([])` e `toggleByLabel`
  - Critério de pronto: clicar em um projeto expande/colapsa via Tailwind `max-h-0` → `max-h-screen` com CSS transition; itens independentes
  - Confiança: 🟢

- [ ] T-12 — Implementar `toggleAll()`
  - Origem no legado: `Projects.tsx` — botão "Expand All" / "Collapse All"
  - Critério de pronto: se nem todos expandidos → expande todos; se todos expandidos → colapsa todos; label do botão reflete o estado
  - Confiança: 🟢

- [ ] T-13 — Corrigir bug BR-07: migrar accordion de `label` para `id` como chave de expansão
  - Decisão do autor (Pergunta 2): corrigir ✅ — labels únicos definidos em `data/tasks.md#T-03`
  - Origem no legado: `Projects.tsx` — `expanded.includes(label)` com 5 projetos com label duplicado
  - Critério de pronto: `expanded` migrado para usar o campo `id` string (ex: `expanded.includes(project.id)`); `toggleByLabel` renomeado para `toggleById`; `toggleAll()` usa `data.map(p => p.id)` em vez de `data.map(p => p.label)`; cada projeto expande/colapsa independentemente
  - Confiança: 🟢

## Tarefas — Componente FuturePartner

- [ ] T-14 — Implementar componente presentacional com `useTranslation`
  - Origem no legado: `FuturePartner.jsx` — `useTranslation('common')`
  - Critério de pronto: todos os textos da aba Message vêm de `common.json` nos 3 locales; sem state, sem lógica de negócio
  - Confiança: 🟢

- [ ] T-15 — Migrar `FuturePartner.jsx` para TypeScript (opcional — gap do legado)
  - Origem no legado: ADR-003 — arquivo deixado em JSX na migração original
  - Critério de pronto: arquivo renomeado para `FuturePartner.tsx` com tipagem adequada; build sem erros
  - Confiança: 🟢

## Tarefas de Teste

- [ ] TT-01 — Transição de aba: clicar em "Projects" → animação completa → aba ativa muda → GA event disparado
- [ ] TT-02 — Trava de loading: clicar em outra aba durante transição → nenhum efeito
- [ ] TT-03 — Carrossel: scroll na seção alterna fotos 1→2→3 e reverte com scroll para cima
- [ ] TT-04 — Accordion: expandir projeto → detalhes visíveis; colapsar → detalhes ocultos
- [ ] TT-05 — Expand All: todos os projetos expandem em um clique; Collapse All colapsa
- [ ] TT-06 — Toggle educação: "Hide Degrees" oculta UNINOVE e FATEC; "Show Degrees" reexibe
- [ ] TT-07 — i18n: trocar locale para `ja` → textos de FuturePartner em japonês

## Ordem Sugerida

1. T-01 (state) → T-06 (renderização condicional) — base estrutural do orquestrador
2. T-07 a T-09 (Jobs) — aba padrão, valida o fluxo mais comum para recrutadores
3. T-10 a T-13 (Projects) — aba de projetos com accordion
4. T-14 (FuturePartner) — componente simples, sem dependências complexas
5. T-02 a T-03 (transição GSAP + GA) — adicionar animação após conteúdo funcionar estaticamente
6. T-04 e T-05 (carrossel + sticky menu) — scroll-driven effects por último
7. T-15 (migração TS) — opcional, sem bloqueio funcional

- [ ] T-16 — Implementar padrão ARIA Tabs nos botões de aba
  - Decisão do autor (Pergunta 9): implementar ARIA Tabs completo ✅
  - Origem no legado: `HeroDark/index.tsx` — botões sem ARIA
  - Critério de pronto: container recebe `role="tablist"`; cada botão de aba recebe `role="tab"`, `aria-selected={selected === tab}`, `aria-controls={`${tab}-panel`}`; painel de conteúdo recebe `role="tabpanel"`, `id={`${selected}-panel`}`, `aria-labelledby` referenciando o botão ativo
  - Confiança: 🟢

## Lacunas Pendentes (🔴)

- ~~BR-07 (label duplicado): requer aprovação~~ → **Aprovado** ✅
- ~~ARIA: decisão pendente~~ → **Aprovado:** implementar ARIA Tabs completo (✅ Pergunta 9)
- **Cleanup GSAP:** sem evidência de `ScrollTrigger.kill()` no legado. A reimplementação deve definir estratégia de cleanup — usar `gsap.context().revert()` no return do `useLayoutEffect`.
