# Data — Tarefas de Implementação

> Template do arquivo `tasks.md`. Sequência executável para reimplementar a unit a partir do legado.
> Generated by Reversa Writer · 2026-05-17

## Pré-requisitos

- [ ] `lodash` disponível (`_.reverse`)
- [ ] Diretório `src/data/` criado na raiz do projeto
- [ ] Assets de ícone de stacks em `public/img/stacks/` (PNGs/SVGs referenciados em `stacks.json` e `swtools.json`)

## Tarefas

- [ ] T-01 — Criar `src/data/jobs.json` com as 5 entradas profissionais
  - Origem no legado: `src/data/jobs.json`
  - Critério de pronto: 5 entradas com campos `id`, `company`, `role`, `start`, `end` (null para ongoing), `ongoing`, `technologies[]`, `description`; entrada com `ongoing: true` tem `end: null`; ordem cronológica (mais antigo primeiro)
  - Confiança: 🟢

- [ ] T-02 — Criar `src/data/toshi-projects.json` com as 19 entradas de projetos
  - Origem no legado: `src/data/toshi-projects.json`
  - Critério de pronto: 19 entradas com todos os campos do schema; ordem cronológica (mais antigo primeiro); **preservar** as 5 entradas com `label: "requirement"` como estão no legado (não corrigir o bug sem decisão de produto)
  - Confiança: 🟢

- [ ] T-03 — Corrigir bug BR-07 (labels duplicados) — **aprovado pelo autor** ✅
  - Decisão do autor (Pergunta 2): labels aprovados: `"axa-requirement"`, `"nbwf-requirement"`, `"local-file-uploader"`, `"web-template-string"`, `"underwriting-improvement"`
  - Origem no legado: `toshi-projects.json` — 5 entradas com `label: "requirement"`
  - Critério de pronto: cada uma das 5 entradas recebe o label aprovado acima; `Projects.tsx` migrado de `expanded.includes(label)` para `expanded.includes(id)` (usando o campo `id` numérico string como chave única); accordion funciona independentemente para cada projeto
  - Confiança: 🟢

- [ ] T-04 — Criar `src/data/stacks.json` com registry de tecnologias
  - Origem no legado: `src/data/stacks.json`
  - Critério de pronto: ~20 entradas com `id`, `name`, `class` (devicon CSS class), `img` (filename em `/public/img/stacks/`); ícones correspondentes presentes em `public/img/stacks/`
  - Confiança: 🟢

- [ ] T-05 — Criar `src/data/swtools.json` com registry de ferramentas
  - Origem no legado: `src/data/swtools.json`
  - Critério de pronto: ~15 entradas com mesmo schema de `stacks.json`; ícones em `public/img/stacks/`
  - Confiança: 🟢

- [ ] T-06 — Adicionar tipos TypeScript para os schemas JSON
  - Origem no legado: ausente — gap identificado
  - Critério de pronto: `src/types/data.ts` com interfaces `JobEntry`, `ProjectEntry`, `StackEntry`; imports tipados em `Jobs.tsx` e `Projects.tsx` (`import data from '...' as JobEntry[]`)
  - Confiança: 🔴 (gap do legado — não existe tipagem no legado)

- [ ] T-07 — Adicionar validação de schema em build time
  - Origem no legado: ausente — gap identificado
  - Critério de pronto: script `validate-data.ts` que valida os 4 JSONs contra os schemas TypeScript antes do build; `package.json` com `"prebuild": "ts-node scripts/validate-data.ts"`
  - Confiança: 🔴 (gap do legado — melhoria proposta)

## Tarefas de Teste

- [ ] TT-01 — `jobs.json`: `_.reverse([...jobs])` em Jobs.tsx → job mais recente aparece primeiro
- [ ] TT-02 — `toshi-projects.json`: 19 projetos renderizados no accordion sem erro
- [ ] TT-03 — Job ongoing: entrada com `ongoing: true` exibe "Present" (sem crash com `end: null`)
- [ ] TT-04 — Bug BR-07 (se T-03 executado): clicar em ex-`"requirement"` projeto → apenas aquele expande
- [ ] TT-05 — Stacks: ícones de tecnologias carregam em `/fullstack-profile/img/stacks/` em produção

## Ordem Sugerida

1. T-01 (jobs.json) — necessário para a aba Profile funcionar primeiro (recrutadores)
2. T-02 (toshi-projects.json) — necessário para a aba Projects
3. T-04 e T-05 (stacks e swtools) — independentes entre si
4. T-06 (tipos TS) — pode ser feito em paralelo com T-01/T-02 para guiar o preenchimento
5. T-03 (correção do bug) — apenas após aprovação do autor; último para evitar mudanças em dados durante desenvolvimento
6. T-07 (validação de schema) — melhoria; pode ser adicionado após tudo funcionar

## Lacunas Pendentes (🔴)

- ~~T-03 (bug BR-07): requer aprovação~~ → **Aprovado** — labels definidos pelo autor (✅ Respondida — Pergunta 2)
- **T-06 e T-07 (tipagem e validação):** ambos são ausentes no legado. Implementar na reimplementação para evitar regressões futuras — mas são melhorias, não requisitos para funcionar.
- **Campo `images[]`:** presente no schema mas sem uso confirmado na UI. Investigar se era para uma galeria de screenshots planejada mas não implementada. Manter no schema por compatibilidade; não renderizar sem decisão de produto.
