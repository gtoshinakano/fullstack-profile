# C4 — Level 2: Containers

> Generated by Reversa Architect · 2026-05-17
> Re-extracted · 2026-05-20 (includes Feature 006 — Tech Stack Opinions)
> Confidence: 🟢 CONFIRMED | 🟡 INFERRED
>
> Note: Feature 006 is integrated into the Worker container (system prompt building with tech opinions). See `deployment-architecture.md` for detailed data flow.

---

## Diagram

```mermaid
C4Container
    title Container Diagram — fullstack-profile

    Person(visitor, "Visitor", "Recruiter, peer, or reader")
    Person(author, "Author", "Gabriel Toshinori Nakano")

    System_Boundary(portfolio_sys, "fullstack-profile") {

        Container(static_site, "Static Site", "Next.js 14 / React 18 / Tailwind CSS", "Hydrated React SPA served as pre-built HTML+JS+CSS from GitHub Pages. No server component at runtime.")

        Container(build_pipeline, "Build Pipeline", "GitHub Actions / Node.js 18", "Executes `npm run build` to produce the /out static export, then deploys to gh-pages branch via JamesIves/github-pages-deploy-action.")

        ContainerDb(public_assets, "Static Assets", "GitHub Pages / gh-pages branch", "All HTML, JS, CSS, images, and translation JSON files. Served directly by GitHub Pages CDN.")

        ContainerDb(json_data, "JSON Data Layer", "Static files bundled at build time", "jobs.json (5 entries), toshi-projects.json (19 entries), stacks.json, swtools.json. Read by React components at runtime via import.")

        ContainerDb(locales, "Translation Files", "/public/locales/{en,ja,pt-BR}/common.json", "i18n strings loaded at build time via serverSideTranslations. Served as static assets.")
    }

    System_Ext(github_pages_cdn, "GitHub Pages CDN", "Serves static files at gtoshinakano.github.io/fullstack-profile/")
    System_Ext(ga4, "Google Analytics 4", "Event tracking")
    System_Ext(pace_cdn, "Pace.js CDN", "Page load indicator script")
    System_Ext(unicons_cdn, "Unicons CDN", "Icon font CSS")
    System_Ext(gfonts, "Google Fonts", "Syne typeface")

    Rel(visitor, static_site, "Visits portfolio", "HTTPS / Browser")
    Rel(author, build_pipeline, "Triggers on push to main", "git push → GitHub Actions")

    Rel(build_pipeline, public_assets, "Deploys /out/ folder contents", "JamesIves Deploy Action")
    Rel(public_assets, github_pages_cdn, "Hosted on", "")
    Rel(github_pages_cdn, static_site, "Serves HTML/JS/CSS/JSON to browser", "HTTPS")

    Rel(static_site, json_data, "Imports at build time / reads at runtime", "ES module import")
    Rel(static_site, locales, "Loads i18n strings", "next-i18next / static fetch")

    Rel(static_site, ga4, "Sends tracking events", "HTTPS / gtag.js (prod only)")
    Rel(static_site, pace_cdn, "Loads progress indicator", "HTTPS / script tag")
    Rel(static_site, unicons_cdn, "Loads icon CSS", "HTTPS / link tag")
    Rel(static_site, gfonts, "Loads Syne font", "HTTPS / link tag")
```

---

## Container Detail

### Static Site
- **Technology:** Next.js 14 (Pages Router) + React 18.3 + TypeScript 5.4 (partial) + Tailwind CSS 3.4 + GSAP 3.12
- **Entry point:** `pages/[locale]/dev/gabriel-toshinori-nakano/gabriel.tsx`
- **Routing:** Static routes generated via `getStaticPaths` — 3 locales × 1 page = 3 HTML files
- **Redirect logic:** `pages/index.ts` → `useRedirect()` hook → browser locale detection → `router.push(/[locale]/dev/...)`
- **basePath:** `/fullstack-profile` (GitHub Pages sub-path, all asset URLs prepend `prefix`)

### Build Pipeline
- **Trigger:** Any push to `main` branch
- **Steps:** checkout → setup Node 18 → `npm ci` → `npm run build` → deploy `/out` to `gh-pages`
- **Build output:** `/out/` folder with 3 static HTML pages + all assets
- **No environment secrets** — only `NEXT_PUBLIC_BASE_PATH` (public, non-sensitive)

### JSON Data Layer
- **Location:** `src/data/` — imported directly by React components (not fetched at runtime)
- **No ORM, no migrations, no database** — plain JSON files edited by the author and committed
- **Known issue:** `toshi-projects.json` has 5 entries with duplicate `label: "requirement"` (see BR-07)

### Translation Files
- **Location:** `public/locales/{en,ja,pt-BR}/common.json`
- **Loading:** `serverSideTranslations` runs at `next build` time (despite the name — it's a build-time function in static export mode)
- **Coverage:** most UI strings translated; `Introduction.tsx` article is English-only
