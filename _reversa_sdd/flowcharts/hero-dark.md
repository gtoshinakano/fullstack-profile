# Flowchart — hero-dark

> Generated by Reversa Archaeologist · 2026-05-17

## Tab Selection State Machine

```mermaid
stateDiagram-v2
    [*] --> job : initial (default)
    
    job --> transitioning : onSelect('projects'|'partner')
    projects --> transitioning : onSelect('job'|'partner')
    partner --> transitioning : onSelect('job'|'projects')
    
    transitioning --> job : complete + newSelected='job'
    transitioning --> projects : complete + newSelected='projects'
    transitioning --> partner : complete + newSelected='partner'
```

## onSelect() Flow

```mermaid
flowchart TD
    A[User clicks menu button] --> B{loading?}
    B -->|true| Z[Ignore — button disabled]
    B -->|false| C[loading = true]
    C --> D[GSAP: fade .should-hide to opacity 0]
    D --> E[GSAP: scrollTo #profile-photo]
    E --> F[onComplete callback]
    F --> G[selected = newSelected]
    F --> H[loading = false]
    F --> I[ga.event menu_click\ncategory: newSelected]
```

## Profile Photo Carousel (ScrollTrigger)

```mermaid
flowchart TD
    A[ScrollTrigger on #profile-photo\nscrub:1, start:'top center', end:'10% 10%'] --> B
    B[#profile-photo-01 fades to opacity 0] --> C
    C[#profile-photo-02 fades in] --> D
    D[#profile-photo-02 fades to opacity 0] --> E
    E[#profile-photo-03 fades in]
```

## Projects Accordion State

```mermaid
flowchart TD
    A[Projects renders\nexpanded = empty array] --> B
    
    B[User clicks 'Expand All'] --> C{allExpanded?}
    C -->|No| D[expanded = all project labels]
    C -->|Yes| E[expanded = empty array]

    F[User clicks project 'Read'] --> G{label in expanded?}
    G -->|No| H[expanded.push label]
    G -->|Yes| I[expanded.splice label out]
    
    H --> J[max-h-0 → max-h-screen\nTailwind transition]
    I --> K[max-h-screen → max-h-0\nTailwind transition]
```

## Menu Sticky Expansion (ScrollTrigger)

```mermaid
flowchart LR
    A[User scrolls past menu] --> B[ScrollTrigger pin fires]
    B --> C[GSAP: width expands to\n  mobile: 100vw\n  desktop: 850px]
    B --> D[scale: 1.1]
    B --> E[border-radius: 0]
    B --> F[duration: 0.7s]
```
