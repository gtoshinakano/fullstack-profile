# Flowchart — layout & data

> Generated by Reversa Archaeologist · 2026-05-17

## Page Assembly

```mermaid
flowchart TD
    A[gabriel.tsx page] --> B[PublicLayout title='FULL STACK DEV...']
    B --> C[Head: title + favicon + webmanifest]
    B --> D[Unicons CDN stylesheet link]
    B --> E[ChangeLanguage fixed widget]
    B --> F[children slot]
    F --> G[HeroSection.WideScreen or Mobile]
    F --> H[HeroDark]
    F --> I[MainContent]
```

## ChangeLanguage Navigation

```mermaid
flowchart LR
    A[User clicks language button] --> B{Which locale?}
    B -->|English| C[router.push prefix + '/en']
    B -->|Japanese| D[router.push prefix + '/ja']
    B -->|Portuguese| E[router.push prefix + '/pt-BR']
    C --> F[Full page navigation\nTranslations reload]
    D --> F
    E --> F
```

## Asset Path Resolution

```mermaid
flowchart TD
    A[Component references image] --> B[import prefix from '@/helpers/prefix']
    B --> C{NODE_ENV}
    C -->|production| D[prefix = '/fullstack-profile']
    C -->|development| E[prefix = '']
    D --> F[Final URL: /fullstack-profile/img/...]
    E --> G[Final URL: /img/...]
    
    H[next/image src prop] --> I[customLoader called]
    I --> J[Returns src unchanged\nBypasses Next.js optimization]
```

## Data Flow: Jobs Tab

```mermaid
flowchart LR
    A[jobs.json] --> B[Jobs component]
    C[stacks.json] --> B
    D[swtools.json] --> B
    B --> E[lodash reverse array]
    E --> F[Render timeline\nnewest first]
    F --> G{hasEducation?}
    G -->|Yes| H[Insert FATEC at index 3\nInsert UNINOVE at bottom]
    G -->|No| I[Skip education entries]
```

## Data Flow: Projects Tab

```mermaid
flowchart LR
    A[toshi-projects.json] --> B[Projects component]
    C[stacks.json] --> B
    B --> D[lodash reverse array]
    D --> E[Render accordion list\nnewest first]
    E --> F{label in expanded[]}
    F -->|Yes| G[max-h-screen\nshow detail]
    F -->|No| H[max-h-0\nhide detail]
```
