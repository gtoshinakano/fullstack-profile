# Toshi AI Terminal — Worker URL
# Local dev: run `cd worker && wrangler dev` first, then use http://localhost:8787
# Production: set to https://fullstack-profile-ai.<YOUR-ACCOUNT>.workers.dev
NEXT_PUBLIC_TOSHI_AI_WORKER_URL=http://localhost:8787
